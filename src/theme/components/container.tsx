import { defineStyleConfig } from '@chakra-ui/react'

export const containerTheme = defineStyleConfig({
  baseStyle: {
    paddingX: ['4','8'],
    paddingTop: '0',
    paddingBottom: '0',
    maxW: 'container.lg'
  },
})