// index.d.ts

// Import types from dependencies if available
import { Theme } from '@chakra-ui/react';
import { FC } from 'react';

// If your theme has a specific shape, you can define it here
export const theme: Theme;

// Define the type for TribalFinanceDLSProvider component
export const TribalFinanceDLSProvider: FC;
